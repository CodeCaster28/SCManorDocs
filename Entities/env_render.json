{
  "Classname": "env_render",
  "Description": "Point entity which allows you to change render-mode, -fx, -amount and -color of another entity. This entity supports use of !activator and !caller.",
  "KeyDescriptions": [
    {
      "keyName": "spawnflags",
      "keyDescription": "SpawnflagsEmpty",
      "keyChoices": [
        {
          "choiceValue": "1",
          "choiceDescription": "If set, the env_render won't change its targets render-fx."
        },
        {
          "choiceValue": "2",
          "choiceDescription": "If set, the env_render won't change its targets render-amt."
        },
        {
          "choiceValue": "4",
          "choiceDescription": "If set, the env_render won't change its targets render-mode."
        },
        {
          "choiceValue": "8",
          "choiceDescription": "If set, the env_render won't change its targets render-color."
        },
        {
          "choiceValue": "16",
          "choiceDescription": "If set, the env_render will search for its target within the provided radius and apply the changes to every valid entity found, whenever it is found, regularly instead of only when triggered."
        }
      ]
    },
	{
      "keyName": "targetname",
      "keyDescription": "Set name of {{ entname }} so other entities can trigger it to apply render mode. Every trigger use-type works uniformly. Can be kill-targeted but already applied render settings stays.",
      "keyChoices": []
    },
    {
      "keyName": "armorvalue",
      "keyDescription": "Set the radius, in units, in which to search for the env_render's targets. Set this to 0 if you do not require the targets to be in a specific radius, but anywhere on the map.",
      "keyChoices": []
    }
  ]
}